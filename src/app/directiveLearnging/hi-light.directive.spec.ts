import { HiLightDirective } from './hi-light.directive';

describe('HiLightDirective', () => {
  it('should create an instance', () => {
    const directive = new HiLightDirective();
    expect(directive).toBeTruthy();
  });
});
